<UserControl x:Class="IsblCheck.Views.Panels.StartPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <StackPanel Margin="24,24" MaxWidth="800" HorizontalAlignment="Center">
      
      <!-- Header -->
      <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="12" Padding="24" Margin="0,0,0,16">
        <StackPanel>
          <TextBlock Text="ISBL Check" FontSize="28" FontWeight="Bold" Foreground="#005A9E" HorizontalAlignment="Center"/>
          <TextBlock Text="Статический анализатор кода ISBL для Directum 5.8" FontSize="14" Foreground="#666666" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
      </Border>

      <!-- Quick Actions -->
      <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
        <StackPanel>
          <TextBlock Text="БЫСТРЫЙ СТАРТ" FontSize="12" FontWeight="Bold" Foreground="#005A9E" Margin="0,0,0,12"/>
          
          <Button Command="{Binding OpenPackageCommand}" HorizontalAlignment="Stretch" Padding="12,8" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
              <Image Source="/Resources/AddPackage_16.png" Width="16" Height="16" VerticalAlignment="Center"/>
              <TextBlock Text="Открыть пакет разработки ISX" FontSize="14" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
          </Button>
          
          <Button Command="{Binding OpenDatabaseCommand}" HorizontalAlignment="Stretch" Padding="12,8" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
              <Image Source="/Resources/AddDatabase_16.png" Width="16" Height="16" VerticalAlignment="Center"/>
              <TextBlock Text="Подключиться к базе данных..." FontSize="14" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
          </Button>
          
          <Button Command="{Binding OpenFolderCommand}" HorizontalAlignment="Stretch" Padding="12,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
              <Image Source="/Resources/AddFolder_16.png" Width="16" Height="16" VerticalAlignment="Center"/>
              <TextBlock Text="Открыть папку с разработкой..." FontSize="14" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
          </Button>
        </StackPanel>
      </Border>

      <!-- Recent Files -->
      <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
        <StackPanel>
          <TextBlock Text="НЕДАВНИЕ ПРОЕКТЫ" FontSize="12" FontWeight="Bold" Foreground="#005A9E" Margin="0,0,0,8"/>
          <TextBlock Text="{Binding RecentFilesMessage}" FontSize="12" Foreground="#999999" TextWrapping="Wrap"/>
        </StackPanel>
      </Border>

      <!-- Version Info -->
      <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="Версия 3.0.5 | Directum 5.8" FontSize="12" Foreground="#A0A0A0"/>
          <TextBlock Text=" | " FontSize="12" Foreground="#D0D0D0"/>
          <TextBlock Text="61 правила анализа" FontSize="12" Foreground="#A0A0A0"/>
        </StackPanel>
      </Border>

    </StackPanel>
  </ScrollViewer>
</UserControl>